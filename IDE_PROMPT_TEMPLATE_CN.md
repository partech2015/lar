# Lár 代理请求模板

**目标**：[描述代理应该做什么，例如，“分析 PDF 并提取财务报表”]

**输入项**：
- [例如：PDF 文件路径]
- [例如：用户查询]

**所需工具**：
- [例如：PDF 解析器]
- [例如：搜索工具]

**约束条件**：
- [ ] 使用 `gemini-1.5-pro` 进行推理。
- [ ] 必须支持物理隔离（气隙）环境（除了本地 LLM 之外没有外部 API）。
- [ ] 输出必须是符合 `FinancialReport` 模式的有效 JSON。

---
**给 IDE 的指令**：
参考 `@lar/IDE_MASTER_PROMPT.md` 以获取编码标准。
在名为 `agent.py` 的单个文件中为此代理生成 `lar` 代码。
底部需包含一个验证块以立即运行。
